// void represents the lack of any type
// the only recommended use for void is as the return type for functions that don't return any value
const log = (msg: string): void => {
  console.log(msg);
};
